# 约束文件

存放 FPGA 工程的引脚与时序约束。

- Vivado 的约束后缀为 `.xdc`
- Quartus / Intel 的约束后缀为 `.sdc`

建议按**板卡**或**工程**分文件夹存放，便于复用：

```
constraints/
  ├── <板卡名>/          # 按板卡放通用引脚约束
  │   └── pin_map.xdc
  └── <工程名>/          # 某个工程专属的时序约束
      └── timing.xdc
```

> 约束文件里一般只写引脚分配和时序要求，不要混入功能性代码。
